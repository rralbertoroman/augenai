# ── API security (required; must match apps/web/.env) ──
AI_PREDICTION_SERVICE_SECRET_KEY=your_shared_secret_key

# ── Server (optional — defaults shown) ──
# APP_NAME=AI Service
# APP_VERSION=0.1.0
# DEBUG=false
# HOST=0.0.0.0
# PORT=8000
# WEIGHTS_DIR=weights

# ── Model inference (optional — defaults shown) ──
# CONFIDENCE_THRESHOLD=0.70
# ULTRALYTICS_IMGSZ=640
# IOU_THRESHOLD=0.70
